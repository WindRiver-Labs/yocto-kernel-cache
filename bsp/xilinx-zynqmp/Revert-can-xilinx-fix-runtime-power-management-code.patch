From 8eceef8259dfa4f63ab3ee8da9f18220200faebf Mon Sep 17 00:00:00 2001
From: Limeng <Meng.Li@windriver.com>
Date: Tue, 19 Nov 2019 14:17:42 +0800
Subject: [PATCH 10/40] Revert "can: xilinx: fix runtime power management code"

This reverts commit 9e86680fc62bfa7e33583a69bb98f6f7594f2f5a.
---
 drivers/net/can/xilinx_can.c | 1 -
 1 file changed, 1 deletion(-)

diff --git a/drivers/net/can/xilinx_can.c b/drivers/net/can/xilinx_can.c
index 2e66870e90b8..07f1fe5c9c8a 100644
--- a/drivers/net/can/xilinx_can.c
+++ b/drivers/net/can/xilinx_can.c
@@ -1750,7 +1750,6 @@ static int xcan_probe(struct platform_device *pdev)
 
 	pm_runtime_set_active(&pdev->dev);
 	pm_runtime_enable(&pdev->dev);
-	pm_runtime_get_sync(&pdev->dev);
 
 	if (priv->read_reg(priv, XCAN_SR_OFFSET) != XCAN_SR_CONFIG_MASK) {
 		priv->write_reg = xcan_write_reg_be;
-- 
2.17.1

