From 11879ee8fe69199ae6fb425a2bf47e704c0f165d Mon Sep 17 00:00:00 2001
From: Camelia Groza <camelia.groza@nxp.com>
Date: Tue, 29 Oct 2019 16:50:34 +0200
Subject: [PATCH 19/30] sdk_dpaa: ls1043a errata: re-enable SG support

commit f7d36b75ba8bc9a488a116bd350260ceae05cb61 from
http://source.codeaurora.org/external/qoriq/qoriq-components/linux

With certain limitation, SG frames can be used safely without triggering
the errata.

Signed-off-by: Camelia Groza <camelia.groza@nxp.com>
Signed-off-by: Zhantao Tang <zhantao.tang@windriver.com>
---
 drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth.c | 12 ------------
 1 file changed, 12 deletions(-)

diff --git a/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth.c b/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth.c
index c9c8830a02d0..dbde2d64764f 100644
--- a/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth.c
+++ b/drivers/net/ethernet/freescale/sdk_dpaa/dpaa_eth.c
@@ -778,18 +778,6 @@ static int dpa_private_netdev_init(struct net_device *net_dev)
 	/* Advertise NETIF_F_HW_ACCEL_MQ to avoid Tx timeout warnings */
 	net_dev->features |= NETIF_F_HW_ACCEL_MQ;
 
-#ifndef CONFIG_PPC
-	/* Due to the A010022 FMan errata, we can not use contig frames larger
-	 * than 4K, nor S/G frames. We need to prevent the user from setting a
-	 * large MTU. We also stop advertising S/G and GSO support.
-	 */
-	if (unlikely(dpaa_errata_a010022)) {
-		net_dev->max_mtu = DPA_BP_RAW_SIZE;
-		net_dev->hw_features &= ~NETIF_F_SG;
-		net_dev->features &= ~NETIF_F_GSO;
-	}
-#endif
-
 	return dpa_netdev_init(net_dev, mac_addr, tx_timeout);
 }
 
-- 
2.17.1

