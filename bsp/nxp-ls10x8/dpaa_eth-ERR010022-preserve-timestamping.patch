From f8beed13b47782b0e1482dc622cf28bf77779c0f Mon Sep 17 00:00:00 2001
From: Camelia Groza <camelia.groza@nxp.com>
Date: Mon, 22 Jul 2019 14:50:36 +0300
Subject: [PATCH 015/245] dpaa_eth: ERR010022: preserve timestamping

commit f0f54139055c1e6bb1185bdb0c0c090198a57715 from
http://source.codeaurora.org/external/qoriq/qoriq-components/linux.git

Maintain all timestamping fields when copying the skb.

Signed-off-by: Camelia Groza <camelia.groza@nxp.com>
Signed-off-by: Yangbo Lu <yangbo.lu@nxp.com>
Signed-off-by: Zhantao Tang <zhantao.tang@windriver.com>
---
 drivers/net/ethernet/freescale/dpaa/dpaa_eth.c | 9 +++++++++
 1 file changed, 9 insertions(+)

diff --git a/drivers/net/ethernet/freescale/dpaa/dpaa_eth.c b/drivers/net/ethernet/freescale/dpaa/dpaa_eth.c
index 4e0b6ece223a..f10c72e0c73e 100644
--- a/drivers/net/ethernet/freescale/dpaa/dpaa_eth.c
+++ b/drivers/net/ethernet/freescale/dpaa/dpaa_eth.c
@@ -2190,6 +2190,15 @@ static struct sk_buff *dpaa_errata_a010022_prevent(struct sk_buff *skb,
 	}
 	copy_skb_header(nskb, skb);
 
+	/* Copy relevant timestamp info from the old skb to the new */
+	if (priv->tx_tstamp) {
+		skb_shinfo(nskb)->tx_flags = skb_shinfo(skb)->tx_flags;
+		skb_shinfo(nskb)->hwtstamps = skb_shinfo(skb)->hwtstamps;
+		skb_shinfo(nskb)->tskey = skb_shinfo(skb)->tskey;
+		if (skb->sk)
+			skb_set_owner_w(nskb, skb->sk);
+	}
+
 	/* We move the headroom when we align it so we have to reset the
 	 * network and transport header offsets relative to the new data
 	 * pointer. The checksum offload relies on these offsets.
-- 
2.17.1

