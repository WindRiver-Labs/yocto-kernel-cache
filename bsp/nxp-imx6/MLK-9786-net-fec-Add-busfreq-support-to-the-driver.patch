From dcd41887b5e0f9214fe9bad593ffbad8f3ae4d24 Mon Sep 17 00:00:00 2001
From: Fugang Duan <b38611@freescale.com>
Date: Tue, 4 Nov 2014 13:23:34 +0800
Subject: [PATCH 0172/5242] MLK-9786 net: fec: Add busfreq support to the
 driver

commit  f35ce5a87e561cf9c1c0b979e8b4787359ff20f6 from
https://source.codeaurora.org/external/imx/linux-imx.git

Add request_bus_freq() and release_bus_freq() calls to the
various drivers to ensure that the DDR and AHB are the requested
frequency before the driver starts its task.

Signed-off-by: Fugang Duan <B38611@freescale.com>
Signed-off-by: Meng Li <Meng.Li@windriver.com>
---
 drivers/net/ethernet/freescale/fec_main.c |    6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/drivers/net/ethernet/freescale/fec_main.c b/drivers/net/ethernet/freescale/fec_main.c
index d7a9d26..8595554 100644
--- a/drivers/net/ethernet/freescale/fec_main.c
+++ b/drivers/net/ethernet/freescale/fec_main.c
@@ -60,6 +60,8 @@
 #include <linux/regulator/consumer.h>
 #include <linux/if_vlan.h>
 #include <linux/pinctrl/consumer.h>
+#include <linux/pm_runtime.h>
+#include <linux/busfreq-imx.h>
 #include <linux/prefetch.h>
 #include <soc/imx/cpuidle.h>
 #include <linux/mfd/syscon.h>
@@ -3583,6 +3585,8 @@ static void fec_enet_of_parse_stop_mode(struct platform_device *pdev)
 		fep->phy_interface = ret;
 	}
 
+	request_bus_freq(BUS_FREQ_HIGH);
+
 	fep->clk_ipg = devm_clk_get(&pdev->dev, "ipg");
 	if (IS_ERR(fep->clk_ipg)) {
 		ret = PTR_ERR(fep->clk_ipg);
@@ -3860,6 +3864,7 @@ static int __maybe_unused fec_runtime_suspend(struct device *dev)
 	struct fec_enet_private *fep = netdev_priv(ndev);
 
 	clk_disable_unprepare(fep->clk_ipg);
+	release_bus_freq(BUS_FREQ_HIGH);
 
 	return 0;
 }
@@ -3869,6 +3874,7 @@ static int __maybe_unused fec_runtime_resume(struct device *dev)
 	struct net_device *ndev = dev_get_drvdata(dev);
 	struct fec_enet_private *fep = netdev_priv(ndev);
 
+	request_bus_freq(BUS_FREQ_HIGH);
 	return clk_prepare_enable(fep->clk_ipg);
 }
 
-- 
1.7.9.5

