From 42c0e6fea7565bcb3245bb180fa64898dfc1b860 Mon Sep 17 00:00:00 2001
From: Liu Ying <victor.liu@nxp.com>
Date: Thu, 30 Aug 2018 15:44:05 +0800
Subject: [PATCH 4536/5242] MLK-19413-25 gpu: imx: dpu: framegen: Add
 framegen_syncmode_fixup() helper

commit  c50fdaa355b2744e036cc598a77d9c32993c5a31 from
https://source.codeaurora.org/external/imx/linux-imx.git

Bit7 of framegen's SECSTATCONFIG register is used to control
the sync mode fixup logic implemented in framegen.  This patch
adds framegen_syncmode_fixup() helper so that the callers
may enable/disable the fixup logic for a framegen.

Signed-off-by: Liu Ying <victor.liu@nxp.com>
Signed-off-by: Meng Li <Meng.Li@windriver.com>
---
 drivers/gpu/imx/dpu/dpu-framegen.c |   19 +++++++++++++++++++
 include/video/dpu.h                |    1 +
 2 files changed, 20 insertions(+)

diff --git a/drivers/gpu/imx/dpu/dpu-framegen.c b/drivers/gpu/imx/dpu/dpu-framegen.c
index 54ff9d6..06b5b55 100644
--- a/drivers/gpu/imx/dpu/dpu-framegen.c
+++ b/drivers/gpu/imx/dpu/dpu-framegen.c
@@ -434,6 +434,25 @@ void framegen_pkickconfig(struct dpu_framegen *fg, bool enable)
 }
 EXPORT_SYMBOL_GPL(framegen_pkickconfig);
 
+void framegen_syncmode_fixup(struct dpu_framegen *fg, bool enable)
+{
+	struct dpu_soc *dpu = fg->dpu;
+	u32 val;
+
+	if (!dpu->devtype->has_syncmode_fixup)
+		return;
+
+	mutex_lock(&fg->mutex);
+	val = dpu_fg_read(fg, SECSTATCONFIG);
+	if (enable)
+		val |= BIT(7);
+	else
+		val &= ~BIT(7);
+	dpu_fg_write(fg, val, SECSTATCONFIG);
+	mutex_unlock(&fg->mutex);
+}
+EXPORT_SYMBOL_GPL(framegen_syncmode_fixup);
+
 void framegen_sacfg(struct dpu_framegen *fg, unsigned int x, unsigned int y)
 {
 	mutex_lock(&fg->mutex);
diff --git a/include/video/dpu.h b/include/video/dpu.h
index 0cf4671..96202855 100644
--- a/include/video/dpu.h
+++ b/include/video/dpu.h
@@ -613,6 +613,7 @@ void fetchwarp_rgb_constantcolor(struct dpu_fetchunit *fu,
 		       struct drm_display_mode *m, bool side_by_side,
 		       bool encoder_type_has_tmds, bool encoder_type_has_lvds);
 void framegen_pkickconfig(struct dpu_framegen *fg, bool enable);
+void framegen_syncmode_fixup(struct dpu_framegen *fg, bool enable);
 void framegen_sacfg(struct dpu_framegen *fg, unsigned int x, unsigned int y);
 void framegen_displaymode(struct dpu_framegen *fg, fgdm_t mode);
 void framegen_panic_displaymode(struct dpu_framegen *fg, fgdm_t mode);
-- 
1.7.9.5

